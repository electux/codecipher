name: codecipher toc generator
on:
  push:
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: codecipher toc generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2
